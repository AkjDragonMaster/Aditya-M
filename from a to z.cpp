#include <stdio.h>

int main() {
    for(char alpha = 97;alpha <= 122;alpha++){
        printf("%c \n", alpha);
			}
	for(char ALPHA=65; ALPHA <= 90; ALPHA++){
	    printf("%c \n", ALPHA);
	}
    
    return 0;
}
